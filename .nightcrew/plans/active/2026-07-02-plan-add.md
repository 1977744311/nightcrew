---
id: 2026-07-02-plan-add
title: Add plan scaffold command
created: 2026-07-02
parallel: false
---

## Goal
Close the operator queueing seam by adding `nightcrew plan add <title>` so operators can create schema-valid active plans from the CLI instead of hand-writing frontmatter and skeleton sections. This is the next authorized backlog item after the completed doctor preflight plan.

## Acceptance
- [ ] `nightcrew plan add <title>` creates `.nightcrew/plans/active/<date>-<slug>.md` with schema-valid frontmatter and Goal, Acceptance, and Steps skeleton sections.
- [ ] Duplicate generated ids are rejected without overwriting an existing plan, and successful creation prints the created path using existing CLI output helpers.
- [ ] Tests cover successful creation, slug/id generation, schema-valid content, printed output, and duplicate rejection.

## Steps
1. Inspect the existing CLI command layout, plan schema, file helpers, and test harness for matching patterns.
2. Implement the `plan add` subcommand with date plus title slug id generation, active plan file creation, duplicate detection, and consistent success/error output.
3. Add focused tests for creation, validation-friendly content, output, and duplicate rejection.
4. Update `CHANGELOG.md` under `## Unreleased` and run the relevant Biome and vitest checks.
